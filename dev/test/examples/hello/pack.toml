name = "wasmmod-test-hello"
version = "0.1.0"
type = "package"

[[target]]
kind = "upy"
path = "hello.py"

# Embed pack sources into wasmmod.source (CDN Inspect / locations).
[source]
embed = true
compress = true


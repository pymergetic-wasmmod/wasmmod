/* pymergetic.util.pysample — umbrella: the canonical include for this
 * module, matching path == module. #include "pysample/__exports__.h"
 * directly still compiles, but that reaches past this module's real
 * path into its internal face layout — depend on this file, not that
 * one. Real body is Python (see pysample/__init__.py) — invisible from
 * here either way. */
#ifndef PYMERGETIC_UTIL_PYSAMPLE_H
#define PYMERGETIC_UTIL_PYSAMPLE_H

#include "src/pymergetic/util/pysample/__exports__.h" // IWYU pragma: export

#endif /* PYMERGETIC_UTIL_PYSAMPLE_H */

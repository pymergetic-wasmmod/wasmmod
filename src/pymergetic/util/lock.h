/* pymergetic.util.lock — umbrella: the canonical include for this
 * module, matching path == module. #include "lock/__exports__.h"
 * directly still compiles, but that reaches past this module's real
 * path into its internal face layout — depend on this file, not that
 * one. */
#ifndef PYMERGETIC_UTIL_LOCK_H
#define PYMERGETIC_UTIL_LOCK_H

#include "src/pymergetic/util/lock/__exports__.h" // IWYU pragma: export

#endif /* PYMERGETIC_UTIL_LOCK_H */

/* pymergetic.types — umbrella: the canonical include for this module
 * (recombines types + export), matching path == module. #include
 * "types/__exports__.h" directly still compiles, but that reaches past
 * this module's real path into its internal face layout — depend on
 * this file, not that one. */
#ifndef PYMERGETIC_TYPES_H
#define PYMERGETIC_TYPES_H

#include "pymergetic/types/__exports__.h" // IWYU pragma: export

#endif /* PYMERGETIC_TYPES_H */

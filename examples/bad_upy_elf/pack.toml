# Negative example: ELF pack that imports micropython.* (unsupported).
# Built only for test-elf; not published to packs/.

type = "package"
name = "pymergetic.wasmmod_examples.badupy"
version = "0.0.1"
impl = ["c"]
comment = "Expect load failure: micropython.* not supported"

[[exports]]
func = "boom"
sig = "i32"

[[imports]]
module = "micropython.runtime"
func = "sched"

[source]
embed = true
compress = true

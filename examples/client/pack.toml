# Depends on the hello pack at runtime (guest→guest via wasmmod.imports).
type = "package"
name = "client"
version = "0.1.0"
impl = ["c"]
comment = "Calls hello.hello through a Wasm import forwarder."

# Defaults: native.dir = "src" (src/client.c).

[lifecycle]
load = "mp_pack_load"
unload = "mp_pack_unload"

[[exports]]
func = "use_hello"

[[imports]]
module = "hello"
func = "hello"

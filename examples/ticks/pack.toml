# Positive micropython.* catalog (Wasm twin of ticks_elf).

type = "package"
name = "pymergetic.wasmmod_examples.ticks"
version = "0.1.0"
impl = ["c"]
comment = "Wasm pack importing micropython.runtime.ticks_ms (host catalog)"

[[exports]]
func = "elapsed"
sig = "i32"

# Imports (micropython.runtime.ticks_ms) are auto-discovered from the
# compiled wasm's own import section — no manifest declaration needed.

# Embed pack sources into wasmmod.source (CDN Inspect / locations).
[source]
embed = true
compress = true

